# Commit 8

Added a shared workflow for agent-created commits. Agents now create a numbered Markdown summary under `_docs/commits`, amend it into the original commit, avoid overwriting existing summaries, and continue to require an explicit request before pushing.
